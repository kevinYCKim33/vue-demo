<%= content_tag :div,
  id: "team-form",
  data: {
    id: team.id, # id is not an accepted parameter from the rails strong_params land...so just extract it out here and add an except :id in the next line
    team: team.to_json(except: [:id, :created_at, :updated_at]), #where rails meets vue team = @team and team.json railsifically outputs the team's attributes in json form
    players_attributes: team.players.to_json(except: [:team_id, :created_at, :updated_at]) #team has many players, and rails associations get beautifully brought out to the front end...
  } do %>

<% end %>
